//pseudocode

/*on login find out if parent or child 
get some indication if parent or child 
use the information to tell if parent or child to go to different partials 
partial for parent and partial for child 
in index.handlebars would have an if stmnt that says if parent...use parent partial and if child...use child partial*/

var x = 1;